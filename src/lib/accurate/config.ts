import {companies,isCompanyId} from '../companies.js'
import type {AccurateCompany} from './types.js'
export const ACCURATE_NOT_CONFIGURED_MESSAGE='Konfigurasi Accurate untuk perusahaan ini belum tersedia.'
export function normalizeCompany(value:string|null):AccurateCompany{if(value&&isCompanyId(value))return value;throw new Error('Company tidak valid. Gunakan perusahaan dari master company.')}
export function validateAccurateHost(host:string){const url=new URL(host.startsWith('http')?host:`https://${host}`);if(url.protocol!=='https:'||!url.hostname.endsWith('.accurate.id'))throw new Error('Host Accurate tidak valid.');return url.origin}
export function getAccurateConfig(company:AccurateCompany){const prefix=companies.find(item=>item.id===company)?.accurateEnvPrefix;if(!prefix)return {ok:false as const,message:ACCURATE_NOT_CONFIGURED_MESSAGE};const apiToken=process.env[`${prefix}_API_TOKEN`];const signatureSecret=process.env[`${prefix}_SIGNATURE_SECRET`];const host=process.env[`${prefix}_HOST`];if(!apiToken||!signatureSecret||!host)return {ok:false as const,message:ACCURATE_NOT_CONFIGURED_MESSAGE};return {ok:true as const,company,apiToken,signatureSecret,host:validateAccurateHost(host)}}
